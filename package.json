{
  "name": "advancement-calculator",
  "version": "1.0.0",
  "description": "Card enhancement calculator for Gloomhaven.",
  "scripts": {
    "start": "rimraf dist && parcel index.html",
    "build": "rimraf dist .parcel-cache && parcel build --public-url . index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gunter Wambaugh",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.0.1",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "bulma": "^0.9.3"
  }
}
